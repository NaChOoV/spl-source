from typing import Dict, Any, List
from app.models.responses import UserResponse, CreateDataRequest, CreateDataResponse


class UserService:
    """Service layer for user-related operations"""

    # In a real application, this would be a database or external service


# Create service instances
user_service = UserService()
